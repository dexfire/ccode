#include <cmath>
#include <iostream>
using namespace std;
const int MAX = 100005;
int N, K;
int less_times, more_times;

void locate() {
  less_times = 1;
  for (int i = 1; N * t < K; i++) {
    less_times *= 2;
    }
    more = less_times * 2;
}

int main() {
    while (cin >> N >> K) {
    }
    return 0;
}