#include <cstdio>

int t, n, m;
int q;
int main() {
    freopen("candy.txt", "r", stdin);
    scanf("%d", &t);
    for (int i = 0; i < t; i++) {
        scanf("Sweet %d");
        scanf("%d", &q);
        for (int j = 0; j < q; j++) {
            scanf()
        }
    }
}