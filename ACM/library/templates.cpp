#include <cstdio>

#define max(a, b) (a > b ? a : b)
#define min(a, b) (a < b ? a : b)

int main() {
#ifdef OJ
    freopen("oj.txt", "r", stdin);
    freopen("out.txt", "a+", stdout);
#endif

#ifdef OJ
    fclose(stdin);
    freopen(stdout);
#endif
}